# Local Development Override Example
# Copy this to .env to override default ports for local development
# This is useful when default ports conflict with other Docker containers

# Example: Override ports if defaults are in use
PORT=3001
WS_PORT=8082
VITE_PORT=5174

# Update frontend URLs to match
VITE_WS_URL=ws://localhost:8082
VITE_API_URL=http://localhost:3001